apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "musicManager.fullname" . }}-test-connection"
  labels:
    {{- include "musicManager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "musicManager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
