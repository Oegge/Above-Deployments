apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "above.fullname" . }}-test-connection"
  labels:
    {{- include "above.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "above.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
