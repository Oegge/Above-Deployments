Thanks for installing {{ .Chart.Name }}!

Frontend Service:
  kubectl get svc {{ include "music-stack.fullname" . }}-frontend

Backend Service:
  kubectl get svc {{ include "music-stack.fullname" . }}-backend

On Minikube, you can get a quick URL:
  minikube service {{ include "music-stack.fullname" . }}-frontend --url
  minikube service {{ include "music-stack.fullname" . }}-backend --url

If you enabled ingress on Minikube:
  minikube addons enable ingress
  # add hosts entries (Windows example as Admin):
  # 127.0.0.1 music.local
  # 127.0.0.1 api.music.local
